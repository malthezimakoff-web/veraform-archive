# Veraform research archive (encrypted static build)

Static, encrypted export of the Veraform market research base. `data.enc` is
AES-256-GCM ciphertext; decryption happens in the browser with the shared
access key. Published automatically from the veraform-research repo's
export_static.py. Nothing in this repo is readable without the key.
